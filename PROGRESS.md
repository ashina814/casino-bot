# 開発進捗・引き継ぎメモ（casino-bot v2）

> 新しいセッションはまず **このファイル → WORLD.md → DESIGN_v2.md** の順で読めば文脈を復元できる。
> 作業ブランチ: **v2**（main へは未マージ）。各機能は実装ごとに commit + push 済み。

## ひとことで

ASTERIA サーバー向け「星約の賭場」カジノ Bot。第二通貨**エテル**で完結するカジノ経済圏。
旧『座敷童の賭場』をベースに、世界観・キャラ・全ゲームを刷新し、対人・為替・景品を追加してきた。

## 確定済みの世界観（正典: WORLD.md）

- 世界: ASTERIA（七星＋第八の黒い星の創世神話。賭場は第八の星の光が溜まる場所）
- 賭場: **星約の賭場** / 通貨: **エテル(◈)** / 第一通貨: **Gil**（別Bot=GilBeinBOT管理）
- マスコット: **アステル**（座敷童の生まれ変わり＝小さな星）
  - 素の声: 一人称「わたし」/呼称「きみ」/自然な現代口語/気だるげ余裕。**古語禁止**
  - 4モード: 常 / 拗ね / 蝕 / 前世(座敷童=古語、旧セリフ流用)
- 進行3軸: 星位(漂着者→北極星) / 星約7段階(暗→満天) / 星の盟約(知恵·言葉·火=派閥兼属性)
- プール: 星溜まり(JP)・巡りの光(救済) / 奉納: 還光

## 実装済み（完了・push済）

1. **Iter.1 基盤**: 第二通貨エテル、全8ゲーム（巻物/札遊び/暴落/輪盤/賽/丁半/競馬/星脈）
2. **世界観スイープ**: 旧用語(座敷童/小判/妖力/龍脈/神馬…)を一掃、アステルの声に総入れ替え
3. **全8ゲーム星仕様化**: 星辰の巻/星札勝負(七星+黒星8スーツ)/星昇り/運命の星盤/天馬競走/星脈相場
4. **Iter.2 対人ソーシャル**: `/板`(賭場の板・公開市場) `/サシ`(1v1エスクロー) `/囃子`
5. **為替API連携**: `core/gilApi.ts`＋`/両替`(入庫/出庫/残高/履歴)。Gil-bot側が正、casinoはエテル増減のみ。
   requestId冪等・失敗時ロールバック・しきい値承認。**モック検証済み／実キー待ち**
6. **景品ショップ拡充**: 使い切り景品5種(お守り/保険/庇護/二度振り/インサイダー)・装備制・6ゲームにフック
7. **複製VC「卓を立てる」(DESIGN_v2 §7.4)**: `/卓 パネル`(管理者)でパネル設置 → ボタンで用途別VC生成
   (サシ/麻雀/対戦/観戦/雑談)。最後の1人退出で自動削除。空き放置は起動時/10分毎にsweep。
   `temp_voice_channels`で追跡し再起動を跨いで掃除。**要 `GuildVoiceStates` intent / Bot権限 ManageChannels**
   - 生成VCの入室権限は**パネル設置テキストチャンネルに追従**（permissionOverwritesをコピー）
8. **`/丁半` 多人数丁半（BOT自動判定 PvP）**: 胴(BOT)が二賽を振り、客が丁(偶)/半(奇)に分かれて張る。
   締切(自動/手締め)→自動精算。勝ち側が負け側を賭け額比で山分け＋元本返却。**場代3%→星溜まり(JP)**。
   片側のみ＝勝負不成立で全額返金。`chohan_games`/`chohan_bets`、起動時返金復旧。
9. **`/チンチロ対戦` 1v1チンチロ対戦（BOT自動判定 PvP）**: 申込→相手「受ける」→両者エスクロー→
   BOTが両者の賽を同一戦略で振って自動判定→勝者総取り(2×額 −場代3%→JP)。同役は最大5回振り直し、
   決まらねば全額返金。役判定はソロチンチロ流用(`chinchiro/index`の`autoRollHand`/`handRank`/`describeHand`)。
   `dice_duels`、起動時返金復旧。検証: 2万戦で50:50・同役率16%。
10. **ゲーム名の通称化（世界観スイープ転換）**: 「何のゲームか一目で分かる」最優先で、星名→通称へ。
    世界観はアステルのセリフ・演出・embed色で出す方針に転換（WORLD.md §3 のゲーム名表は廃止）。
    - 星辰の巻→スロット / 星札勝負→ブラックジャック / 星昇り→クラッシュ / 運命の星盤→ルーレット /
      天馬競走→競馬 / 星脈相場→株 / サシ星約→サシ勝負 / 星約の板→賭場の板
    - コマンド改名: `/盆`→`/丁半`、`/賽勝負`→`/チンチロ対戦`、`/星脈`→`/株`、`/遊ぶ`サブも通称化
    - ⚠️ 注意: `/遊ぶ 丁半`（highlowフォルダ＝関数名は紛らわしいが**中身は丁半**＝丁/偶半/奇のソロ）。
      多人数版が `/丁半`。チンチロも `/遊ぶ チンチロ`(ソロ) と `/チンチロ対戦`(PvP) で対の構成。
    - **要 redeploy**（全コマンド名が変わったため）
11. **大勝ち速報（`core/bigwin.ts`）**: JP当選 or 高倍率勝ちを `jackpot_channel_id` へ自動投稿。
    発火: JP当選 / 払戻比≥15倍かつ純益≥5,000 / 純益≥100,000。配線済=スロット/クラッシュ/ルーレット/チンチロ。
    - ⏳ 未配線: 競馬(settlePoolにclient未配線・パリミュ)・BJ/ハイロー/PvP(倍率低く発火せず) → 必要なら後続

## インフラ整備（進行中）

**サーバー構成（ASTERIA・既に DESIGN §7 にほぼ沿う）**: 表口(案内所/賭場商店/賭場/遊技場/競馬場/株式市場/卓を立てる/実況席)＋裏口VIP。
洗練の確定方針:
- 対人専用 **#勝負場** を新設（丁半/チンチロ対戦/サシ/板）
- **#遊技場 は Forum 化**し常設卓スレ（スロット卓/BJ卓/…）運用（§7.2.1）
- **場所縛り = Discordの連携(Integration)コマンド権限**で「指定ch以外は非表示」（コード不要・手動設定）
- パネル常設: #案内所→`/案内`、#賭場商店→`/商店`(パネル化TODO)、#卓を立てる→`/卓 パネル`

**Ops 現状**: 🟢再起動耐性OK(pm2-kabu.service enabled+save済) / 🟢日次バックアップ稼働(cron 4時・7日保持) /
🟡速報ch未設定(stock→#株式市場, jackpot→#賭場 を `/管理` で要設定) / ⬜オフサイト退避・ログローテは任意。

**パネル常設化（実装済）**:
- `/案内 設置`(管理者)→ #案内所 に公開常設パネル（静的embed＋全ゲーム/福分け/通行証ボタン）。`/案内 ホーム`=個人ephemeral。
- `/商店 設置`(管理者)→ #賭場商店 に公開常設パネル。ボタン[購入][持ち物][使う]→各自ephemeral。購入UIもephemeral化。
- ⚠️ `/案内` はサブコマンド化（`/案内 ホーム`）。ボタンは共有メッセージを書き換えず各自ephemeralで開くので安全。

**五行（旧GOGYO）撤去（実装済）**: WORLD.md で五行→三星(知恵/言葉/火=星の盟約)に統合済みなのにコードが旧五行のままだったため撤去。
- `/アステル element` サブコマンド削除（`/アステル` は status/mode のみに）。status/通行証/ヘルプ/daily から五行表示・記述を除去。
- 死蔵: `core/zashikiStage.ts` の GOGYO/gogyoMultiplier/getElementAwakeningDialogue、`db.ts` の element系関数＋ users/affection の element 列は**残置（未使用）**。三星＝派閥はS2で本実装する際に整理。

**バグ修正（案内パネル）**: 通行証(options例外)・福分け(非ephemeral)・ヘルプ(`✦`がCOMPONENT_INVALID_EMOJI)を修正。
教訓: **ボタン/セレクトのemojiは標準絵文字限定**（星グリフ ✦✧… はembed本文のみ）。

**経済・バランス調整ログ**（数値の所在も明示。仕様書は無く、値はコードが正）:
- **株の1回投資上限 = `max(betCap, 3000)`**（`games/stocks/index.ts` の `STOCK_TX_FLOOR=3000`）。
  理由: 漂着者の betCap=500 だと最安株(約600)すら買えなかった。投資は単発の賭けと別物として下限を被せる。
- 値の所在: 賭け上限(betCap)＝`core/economy.ts` TIERS（漂着者500/星拾い2000/星約者10000/星詠み50000/北極星100000）。
  min_bet/初期残高/balance_cap 等＝`core/db.ts` server_config 既定値（初期3000・min_bet50・cap300000）。株価シード＝`games/stocks/index.ts`(600〜1500)。
- ⚠️ 今後バランス値を変えたら**このログに1行残す**（散在防止・引き継ぎ用）。
- **救済プール(巡りの光)を還元化**: 福分け時、残高≤1,000の困窮者に救済プールから最大◈500を上乗せ（`economy.drawFromReliefPool`／`daily.ts`）。
  従来 relief_pool は加算のみの死蔵シンクだった → 設計通り弱者へ巡るように。
- **序盤テンポ緩和**: 漂着者 betCap 500→**1,000**、exp曲線係数 100→**75**（段位昇格が約25%早く）。`economy.ts`。
- **景品(称号)価格を為替OFF前提に**: パトロン100k→**30k** / 成金500k→**100k** / 寵児1M→**300k**（=残高上限を頂点）。`games/shop.ts`。

### 経済診断メモ（為替OFF時点）
- 構造は**閉鎖系でデフレ寄り**（mint=福分けのみ／sink=ハウス50%消滅＋景品＋累進奉納）。インフレ抑制は多層で堅牢。
- 課題は「序盤グラインド」「高額景品が遠い」「救済の死蔵」→ 上記で対処。
- 未接続の出入口: 為替(Gil流入/換金20%還光)・従業員給与(Gil) は Gil-bot 実キー待ち。繋がると mint/sink 構造が変わるので再診断要。
- 📄 **二通貨経済の財務モデルは別セッションで実施予定** → `ECONOMY.md` に現状報告・狙い・二通貨フロー・Gil側必要データ・次の一手を集約済み。

**プレイヤーUX改善（実装済）**:
- **株購入を「株数」ベースに**（売りと対称）。モーダルは「何株買う？（空欄なら買えるだけ）」、placeholderに `1株◈X / いまは最大N株`。投資額入力＆端数切り捨ての分かりにくさを解消。
- **失敗メッセージを親切に**: 1株も買えない時/上限超過時に「最大N株」「上限が効いてる/所持金が足りない」を具体提示。
- **アステルの口調統一（古語→素の現代口語）**: stocks/blackjack/chinchiro/highlow/keiba/help/quests の素の声に残ってた「〜じゃ/〜ぬ/客人/来い」等を修正。**前世(zense)モードの古語は維持**。
- 教訓: アステル素は古語禁止（WORLD §4）。前世モードのみ古語。

**次の候補**: ①`/管理`で速報ch設定(stock→#株式市場, jackpot→#賭場) ②#勝負場新設+Integration権限ロック(手動) ③遊技場Forum化(手動)

## VIP（奥座敷・月課金エテル）実装メモ
- **`/vip`**: 状態表示＋[加入/更新]ボタン。月会費 **◈30,000 / 30日**（`core/vip.ts` の VIP_PRICE/VIP_DAYS）。
- 加入で `vip_members`(user/guild/expires_at) を延長＋**VIPロール付与**（`server_config.vip_role_id`・要 Bot権限 ManageRoles）。
- 期限切れ: 毎時cron `sweepExpiredVips` がロール剥奪＋行削除。expires_atはISO保存・JSで期限判定。
- 識別: 通行証(profileEmbed)に「💎 VIP会員」表示（profile.ts が isVip を渡す）。
- 奥座敷アクセス: VIPロールに対するDiscordチャンネル権限で制御（手動設定）。
- 設定: `/管理 設定`→📢チャンネル編集 に「VIPロールID」欄を追加。
- ⏳ **未配線: 高bet上限（VIP_BETCAP_MULT=2）**。`getVipBetCapMultiplier` は用意済みだが、betCapは12ファイル63箇所に散在するため未適用。次は betCap を集中化(helper)して VIP×2 を反映する。
- 🔴 **従業員(支配人/ディーラー)は別途・給与Gilのため為替稼働後**（VIPとは別軸）。

## 主要ファイル地図

- `src/world.config.ts` — 世界観の値（名称/色/段階/星位/モード等）
- `src/core/` — db / bank / economy / dialogue(+Data) / zashikiStage(星約段階) / exchange / gilApi / items
- `src/games/` — 各ゲーム + board(板) + sashi + cheer + takutate(卓) + chohan(盆) + saishoubu(賽勝負) + exchange(/両替) + shouten(/商店)
- `src/index.ts` — interaction ルータ + 起動時返金/整合
- `WORLD.md` 正典 / `DESIGN_v2.md` 構造設計 / `~/.claude/plans/ok-bot-clever-balloon.md` 詳細プラン全文

## 未完・次の候補

- ⏳ **為替 実接続テスト**: Gil-bot の APIキー/実URL/テスト用 guildId・userId が来たら .env に入れて疎通
  （env: `EXCHANGE_API_BASE_URL` `EXCHANGE_API_KEY`。未設定なら /両替 は「準備中」で安全）
- ⬜ **Discord実機スモーク**: `npm run deploy` でスラッシュ登録 → 各機能を実際に触る（未実施）
  - ⚠️ コマンド改名あり: `/座敷童`→`/アステル`、`/龍脈`→`/星脈`。要 deploy
- ⬜ **A/E/F系景品**（黄金ネーム・名前飾り等の可視化系）
- ⬜ **Iter.3 派閥**（知恵/言葉/火）= **シーズン2送り**
- ⬜ **Iter.4 従業員(支配人/ディーラー)+VIP** — 給与=Gil なので為替API稼働後
- ✅ **複製VCパネル**（DESIGN_v2 §7.4）→ 上記7で実装。実機スモーク未（VC生成/自動削除を実際に確認したい）
  - 残: **遊技場フォーラム化**（§7.2.1）は常設卓スレッドをDiscordで手動作成する運用でBot側コード不要。要サーバー設定

## 運用メモ

- ビルド: `npm run build`（tsc）。起動: `npm start` / PM2: `ecosystem.config.js`(name=casino-bot)
- Node 24（`node:sqlite` 使用）。DB: `data/database.sqlite`（差分マイグレーションは initializeDatabase 内）
- スモーク検証は `node -e` で initializeDatabase を叩く方式を多用（一時 _*.js は使い捨て）
- LF/CRLF 警告は無害
